---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  creationTimestamp: null
  name: {{ include "ack-quicksight-controller.app.fullname" . }}-reader
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: {{ include "ack-quicksight-controller.app.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/version: {{ .Chart.AppVersion | quote }}
    k8s-app: {{ include "ack-quicksight-controller.app.name" . }}
    helm.sh/chart: {{ include "ack-quicksight-controller.chart.name-version" . }}
rules:
- apiGroups:
  - quicksight.services.k8s.aws
  resources:
  - analyses
  - dashboards
  - datasets
  - datasources
  verbs:
  - get
  - list
  - watch
