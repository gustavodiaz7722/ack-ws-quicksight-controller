---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  creationTimestamp: null
  name: {{ include "ack-quicksight-controller.app.fullname" . }}-writer
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: {{ include "ack-quicksight-controller.app.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/version: {{ .Chart.AppVersion | quote }}
    k8s-app: {{ include "ack-quicksight-controller.app.name" . }}
    helm.sh/chart: {{ include "ack-quicksight-controller.chart.name-version" . }}
rules:
- apiGroups:
  - quicksight.services.k8s.aws
  resources:
  - analyses
  - datasets
  - datasources
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - quicksight.services.k8s.aws
  resources:
  - analyses
  - datasets
  - datasources
  verbs:
  - get
  - patch
  - update
